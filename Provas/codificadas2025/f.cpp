#include<bits/stdc++.h>
using namespace std;


pair<int,int> pontoMedio()
int main(){
    ios::sync_with_stdio(false); 
    cin.tie(NULL); 

    // Xmedio = (x₁ + x₂) / 2
    // Ymedio = y = (y₁ + y₂) / 2
    int n;
    cin >> n;

    return 0;
}

